# QA Plan

| ID | Environment | Preconditions | Test data | Actions | Expected observable result | Cleanup |
|---|---|---|---|---|---|---|
| QA-001 | Python 3.11+, local test runner | Dependencies installed | Mock responses for six sources | Run unit/integration suite | All deterministic tests pass; no network required | Remove test cache |
| QA-002 | Local `127.0.0.1` app | SQLite database initialized | Keyword `nutrition`, years `2020-2025` | Submit search with two sources and OA-only | Results are deduplicated and show source, DOI, year, and OA state | Delete temporary DB |
| QA-003 | Local `127.0.0.1` app | A result has an approved-open PDF/XML candidate | Small valid PDF/XML fixture | Open detail and select download | Response is returned only after magic-byte validation and is cached | Delete cached object |
| QA-004 | Local `127.0.0.1` app | A result has metadata but no open asset | DOI-only fixture | Open detail | Abstract/metadata and legal external link are shown; no bypass URL is offered | Delete temporary DB |
| QA-005 | Local `127.0.0.1` app | SQLite writable | One result | Toggle favorite, download once, reload | Favorite and downloaded list survive reload | Delete temporary DB |
| QA-006 | Python 3.11+, local test runner | Frozen expansion fixtures and labeled result fixtures | `AI客服`, `人工智能客服`, `customer service chatbot` | Run relevance test suite and calculate precision for each query's first 10 results | Each query has at least 80% relevant results; no AI-only false positive is in the first 10; `match_fields` and `matched_terms` are present | Remove fixture output |
| QA-007 | Local API with mocked model client | Model returns valid JSON and provider request is captured | `AI客服` plus fixed expansion response | Submit search through `/api/search` | Six source queries receive source-appropriate expanded terms; final results are filtered and globally ranked | Remove mock logs |
| QA-008 | Local API with failing model client | Model timeout/invalid JSON injected | `AI客服` | Submit search while expansion fails | Raw-query fallback is used; the response contains only gate-approved results and may be empty | Remove mock logs |
| QA-009 | Local API with SQLite cache | Cache contains payload from prior relevance version | Same query before and after version bump | Request without refresh, then with refresh | Old cache is bypassed; new result payload is stored and refresh forces source retrieval while retaining expansion cache | Delete temporary DB |

Traceability: `acceptance.feature` baseline scenarios map to QA-002 through QA-005; precision scenarios map to QA-006 through QA-009; QA-001 covers the shared connector seam and deterministic checks; the download UX content-negotiation scenario maps to QA-010 through QA-011.
| QA-010 | Local `127.0.0.1` app, browser context | A search result with metadata but no approved full-text candidate (e.g. paywalled journal paper) | DOI-only fixture paper | Click 下载 with browser Accept header, then call the endpoint with `Accept: application/json` | Browser request returns an HTML page with Chinese reason (暂无开放全文) and 返回搜索 link, HTTP 404; JSON request returns the same `status`/`detail` JSON contract as before, HTTP 404 | Delete temporary DB |
| QA-011 | Local `127.0.0.1` app | A result whose candidate URL serves invalid bytes (not `%PDF-`) | Broken candidate fixture | Click 下载 with browser Accept header | HTML page shows 下载失败 with the validation reason, HTTP 403; no file is saved to the object store | Delete temporary DB and object store |
