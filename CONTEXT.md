# paper-hub Context

This context defines the language used for paper discovery and result quality.

## Language

**查询意图**:
用户希望检索的主题概念，可能由中文、英文、缩写和领域同义词组成。
_Avoid_: 只把原始关键词当作完整语义。

**相关结果**:
论文标题、摘要或关键词对查询意图提供实质性证据的结果。
_Avoid_: API 命中（API 返回不等于主题相关）。

**相关性门槛**:
在结果展示前判断一篇论文是否足够相关的最低证据要求。
_Avoid_: 为凑数量自动放宽门槛。

**概念扩展**:
由扩展生成器提出、经格式和数量约束后的有限中英文术语和同义词集合；精确短语的优先级高于扩展词。
_Avoid_: 把模型输出直接当成相关性结论，或允许无边界的自动翻译和开放式改写。

**扩展生成器**:
根据用户输入生成查询意图候选词的模型调用；它提供召回线索，不拥有结果准入权。
_Avoid_: 重排器、审核器、最终相关性裁决者。

**稳定评测集**:
包含固定查询、冻结扩展词和人工相关标签的测试数据，用于验证确定性的相关性门槛和排序。
_Avoid_: 把一次实时模型输出当成可重复的精度基准。

**命中字段**:
支持相关性判断的论文字段：标题、摘要或关键词；用于解释结果为何入选。
_Avoid_: 面向用户展示内部数值分数。

**合法全文资产**:
来源以开放许可或已批准方式提供的可下载全文文件，PDF 或 JATS XML 格式。
_Avoid_: 支付墙绕过、盗版镜像、"来源页面上的论文"本身。

**格式优先级**:
同一论文存在多个合法全文资产时单一下载入口的选择次序：PDF 优先，XML 兜底。
_Avoid_: 按来源顺序取第一个、把 XML 当作对等选项让用户自选。

**格式徽标**:
结果卡片上对"这篇将下载到什么"的展示：有确定证据时显示具体格式，无证据时显示中性提示。
_Avoid_: 隐瞒格式让用户下载后发现、虚假承诺每篇都精确标注。

**搜索状态恢复**:
用户离开搜索页再返回时，回到离开时的搜索视图（查询条件与结果列表）。
_Avoid_: 恢复收藏/已下载视图、跨浏览器会话的持久化、"重新搜一遍"的替代品。

**UI 投影**:
结果列表中页面实际渲染的字段子集，区别于包含摘要的完整 API 响应。
_Avoid_: 把投影当缓存——后端搜索缓存才是缓存，投影只为恢复视图。
